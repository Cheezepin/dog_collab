#include "sphere.inc.c"
#include "palette_swap.inc.c"
#include "emu_amp.inc.c"
#include "emu_dog.inc.c"
#include "electric_ring.inc.c"
/*void bhv_Propane_Flame_Emu(void) {
   if((3.0f-o->oTimer*.5) > 2){ 
    obj_scale(o,3.0f-(o->oTimer * .5));
        }
    o->oPosX += sins(o->oMoveAngleYaw) * 50.0f;
    o->oPosZ += coss(o->oMoveAngleYaw) * 50.0f;
    if (o->oTimer > 50) {
        obj_mark_for_deletion(o);
        }
}*/